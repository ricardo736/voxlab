# Beta Build Configuration
# Set to 'true' to enable beta testing features (greeting change, version selector, feedback button)
# Set to 'false' or leave empty for production build
VITE_BETA_BUILD=true

# API Key for Google Gemini (if using AI features)
API_KEY=
